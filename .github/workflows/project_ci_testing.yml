name: Project CI Check (Angular + Spring Boot)

on:
  pull_request:
    branches:
      - main  # Run only when merging to main branch
  push:
    branches:
      - main

jobs:
  # Analyze which folders were changed
  analyze-changes:
    runs-on: ubuntu-latest
    outputs:
      frontend: ${{ steps.filter.outputs.frontend }}
      backend: ${{ steps.filter.outputs.backend }}
      document: ${{ steps.filter.outputs.document }}
    steps:
      - uses: actions/checkout@v4

      - name: Detect changed folders
        id: filter
        uses: dorny/paths-filter@v3
        with:
          filters: |
            frontend:
              - 'frontend/**'
            backend:
              - 'backend/**'
            document:
              - 'document/**'
              - '**/*.pdf'
              - '**/*.yml'

  # Skip check when only documents are changed
  skip-docs:
    runs-on: ubuntu-latest
    needs: analyze-changes
    if: >
      needs.analyze-changes.outputs.document == 'true' &&
      needs.analyze-changes.outputs.frontend != 'true' &&
      needs.analyze-changes.outputs.backend != 'true'
    steps:
      - name: Skip CI
        run: echo "Only documents changed — skipping build and tests."

  # Frontend (Angular)
  frontend-check:
    runs-on: ubuntu-latest
    needs: analyze-changes
    if: needs.analyze-changes.outputs.frontend == 'true'
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/cefrs-frontend/package-lock.json

      - name: Install dependencies
        run: cd frontend/cefrs-frontend && npm ci --legacy-peer-deps

      - name: Build Angular app
        run: cd frontend/cefrs-frontend && npm run build --if-present

      - name: Run tests
        # explicitly use ChromeHeadless for CI compatibility
        run: |
          cd frontend/cefrs-frontend
          npx ng test --watch=false --browsers=ChromeHeadless

  # Backend (Spring Boot w/ Maven)
  backend-check:
    runs-on: ubuntu-latest
    needs: analyze-changes
    if: needs.analyze-changes.outputs.backend == 'true'
    steps:
      - uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: 21

      - name: Build and test with Maven
        run: cd backend/cefrs-backend && mvn -B clean verify
