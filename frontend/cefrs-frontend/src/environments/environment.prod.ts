export const environment = {
  production: true,
  // In production the frontend can use relative paths (empty string) so
  // requests go to the same origin as the served app. If you host backend
  // on a different domain, set the full URL here when building.
  apiBaseUrl: ''
};
